
# Standard Hostinger Node.js configuration
# Node Selector usually handles routing, so we only need basics here.
Options -MultiViews
RewriteEngine On

# Ensure the app handles all routing including SPA fallbacks
# API requests should pass through to server.js
# Static files should be served normally
RewriteCond %{REQUEST_FILENAME} !-f
RewriteRule ^ index.html [QSA,L]
