
RewriteEngine On
RewriteBase /

# Force API calls directly to Node.js
RewriteRule ^(_api_)($|/) - [L]

# If file exists, serve it
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Otherwise, hand over to index.html (SPA)
RewriteRule ^ index.html [L]
